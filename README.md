# Productivist
A simple frontend application with a pomodoro clock and a To-Do list.

## Development

```sh
npm install
npm run dev      # start the dev server
npm run build    # type-check and build for production
```

Built with Angular 19 (standalone components, signals) and TypeScript. To-do list and settings (theme, language, durations) persist to `localStorage`.
